query UserQuery($query: String!){
  search (type: USER, query:$query, first:20) {
    edges {
      node {
        ... on User {
          avatarUrl
          location
          login
          name
        }
      }
    }
  }
}
