query RepositoryQuery ($user: String!){
  user (login: $user) {
    repositories (first:20){
      edges {
        node {
          description
          name
          pullRequests {
            totalCount
          }
        }
      }
    }
  }
}
