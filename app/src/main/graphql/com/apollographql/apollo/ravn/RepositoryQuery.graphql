query RepositoryQuery ($user: String!){
  user (login: $user) {
    repositories (first:15){
      edges {
        node {
          description
          name
          pullRequests {
            totalCount
          }
        }
      }
    }
  }
}
