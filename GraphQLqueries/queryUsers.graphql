query {
  search (type: USER, query:"jair", first:20) {
    edges {
      node {
        ... on User {
          avatarUrl
          location
          login
          name
        }
      }
    }
  }
}
