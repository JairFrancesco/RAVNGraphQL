query {
  user (login:"jairfrancesco") {
    repositories (first:20){
      edges {
        node {
          description
          name
          pullRequests {
            totalCount
          }
        }
      }
    }
  }
}
